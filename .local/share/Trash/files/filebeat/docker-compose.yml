filebeat:
  image: bargenson/filebeat:latest
  restart: unless-stopped
  volumes:
   - /var/run/docker.sock:/tmp/docker.sock
  environment:
   - LOGSTASH_HOST=172.22.0.3
   - LOGSTASH_PORT=5044
   - SHIPPER_NAME=test